# VFL Review 需求说明

## 1. 项目背景

在芯片验证过程中，Verification Feature List（简称 VFL）是重要的验证交付件。VFL 通常以 Excel 表格形式维护，用于记录各个模块需要覆盖的验证特性、测试点及相关信息，并作为验证计划和专家评审的重要输入。

随着项目持续迭代和多人协作维护，现有各模块 VFL 的内容质量、格式一致性和评审效率逐渐出现问题，需要建设一套基于 Skill 和大模型的 VFL 初步检视与评审辅助能力。

## 2. 当前痛点

### 2.1 内容冗余、重复及准确性不足

- 各模块 VFL 数据量较大，单个文件可能达到数千行。
- 历史版本不断迭代，旧内容持续累积，存在大量冗余条目。
- 不同版本或不同位置可能存在重复、近似重复的测试点。
- 部分条目可能已过时、描述不准确，甚至存在错误内容。
- 数据规模较大，人工逐条检查成本高，容易遗漏问题。

### 2.2 文件格式和填写规范不统一

- 早期虽然制定过统一模板，但后续维护过程中未被严格遵守。
- 不同人员的填写习惯存在差异，导致列结构、字段名称、字段取值和内容表达不一致。
- 同一类信息可能出现在不同列中，或使用不同格式表达。
- Excel 中可能存在不一致的空值、合并单元格、字体样式、颜色及其他排版问题。
- 格式不统一增加了后续统计、检索、比较、自动检查和跨模块汇总的难度。

### 2.3 专家评审投入与重点不匹配

- 目前专家需要对各模块 VFL 的大量内容进行人工 review。
- 其中包含大量结构简单、规则明确、风险较低的测试点。
- 简单测试点也需要逐条经过专家确认，消耗大量评审时间。
- 大量低价值的重复性 review 挤占了专家分析复杂场景和高风险内容的时间。
- 缺少有效的分级和筛选机制，难以让专家优先关注真正需要判断和深入讨论的重点内容。

## 3. 项目目标

本项目拟针对各模块 VFL 建立统一、可检查、可解释、可追溯的初步检视和评审辅助流程，达到以下目标：

1. 识别并减少 VFL 中的冗余和重复内容，发现潜在错误或异常条目。
2. 识别不同来源、不同写法的 VFL 与统一模板之间的差异，并提出规范化建议。
3. 通过规则检查和内容分析降低人工检查成本，提高 VFL 的整体质量。
4. 对测试点进行风险或复杂度分级，筛选出需要专家重点 review 的内容。
5. 保留必要的人工确认环节，并通过报告提供清晰的问题定位、分析依据和修改建议。

## 4. 需求范围

### 4.1 输入

- 支持导入各模块现有的 VFL Excel 文件。
- 兼容不同历史版本及不同填写习惯产生的表格结构。
- 支持识别工作表、表头、数据行、空行、备注行及其他非标准内容。

### 4.2 VFL 内容质量检查

系统应能够辅助检查以下问题：

- 完全重复或高度相似的测试点。
- 内容冗余、描述过于泛化或缺少关键信息的条目。
- 字段缺失、字段值非法或字段之间存在明显矛盾的条目。
- 可能过时、无效或疑似错误的内容。
- 测试点之间的覆盖重叠、关联异常或分类不合理问题。

检查结果应能够定位到原始文件、工作表和具体行，并给出问题类型及必要的说明。

### 4.3 VFL 格式规范化

系统应能够辅助识别和提出以下规范化建议：

- 根据统一模板映射和整理字段。
- 统一列名、列顺序、数据类型和字段取值格式。
- 处理空值、重复表头、空行、合并单元格及其他常见表格异常。
- 统一测试点描述及枚举字段的表达方式（在不改变原始语义的前提下）。
- 可选地生成仅供参考的字段映射或示例结果，但不得将其作为正式 VFL 直接替换用户文件。
- 保留原始内容与所有分析意见之间的对应关系，支持追溯。

### 4.4 专家评审辅助

系统应能够根据规则和内容分析结果，对测试点进行分级或分类，例如：

- 可由规则直接确认的简单测试点。
- 存在重复、缺失、冲突或格式异常的测试点。
- 涉及复杂场景、边界条件、关键接口或较高验证风险的测试点。
- 需要专家进一步判断的疑似问题条目。

系统应优先呈现高风险、高复杂度和存在疑问的内容，并为专家提供：

- 重点 review 清单。
- 条目原文、相关上下文及触发原因。
- 检查结果、建议处理方式和人工 review 结论。
- review 状态、责任人和处理记录。

### 4.5 基于 Skill 和大模型的 VFL Review

系统应能够利用预定义的 Skill 和大模型，对单个或多个模块的 VFL 进行初步检视。大模型的结论应作为辅助意见，不能替代专家最终判断。

基础 Review 能力包括：

- 检查 Excel 文件结构、表头、字段、数据类型、必填项和基本格式是否符合统一模板。
- 检查测试点、验证特性及相关描述是否完全重复或高度相似。
- 对相似项目或相近测试点进行对比，指出内容差异、适用范围和可能的冲突，并分析哪个内容更合理。
- 识别疑似错误、过时、矛盾、缺失或表述不清的内容，并说明触发依据。

增强 Review 能力包括：

- 结合模块对应的 Spec、HLD 和历史 Bug 库分析 VFL 覆盖是否完备。
- 根据设计功能、接口、状态、边界条件、异常路径和历史问题，发现可能遗漏的验证特性或测试点。
- 针对发现的缺口给出基础建议、建议关注的验证方向及建议补充的信息。
- 区分有明确证据支持的结论、基于上下文的推断和需要专家确认的疑点。

参考资料的使用要求：

- 支持将 Spec、HLD、历史 Bug 库作为 Review 的参考输入，并标识资料名称、版本或来源。
- 分析意见应尽可能引用对应资料中的章节、条目或 Bug 编号，便于专家复核。
- 当参考资料缺失、版本不明确或内容不足时，应明确标注分析局限，不得臆测为确定结论。

### 4.6 Review 报告输出

Review 的主要交付物是初步检视报告，而不是修改后的 VFL 文件。报告至少应包含：

- 本次 Review 的文件、模块、工作表、资料输入和处理范围。
- 格式问题清单及对应行号、字段、问题描述和建议处理方式。
- 重复或相似内容分组，以及差异对比和待确认项。
- 疑似错误、内容冲突、缺失和覆盖风险清单。
- 基于 Spec、HLD 和历史 Bug 库发现的覆盖缺口及基础建议。
- 按优先级、风险或 Review 类型整理的专家重点关注清单。
- 每条意见的置信度、分析依据和“建议专家确认”的标记。

报告应支持人类阅读，并尽可能提供结构化结果，方便后续筛选、分派和跟踪。

## 5. 处理边界与安全要求

- 不直接修改、覆盖或自动删除用户提供的 VFL 文件。
- 不自动将大模型建议写回 VFL；任何实际修改均由用户或专家自行决定并执行。
- 报告中的建议必须与原始条目、分析依据和不确定性区分展示，避免用户误以为建议已被确认。
- 原始 VFL、Spec、HLD 和历史 Bug 库应保持只读，分析过程不得破坏源文件。

## 6. 非功能要求

- 处理结果应具备可解释性，避免仅给出无法核对的结论。
- 原始 VFL 文件不得被直接覆盖，所有修改应可追溯、可回退。
- 对大规模 Excel 文件应具备稳定的处理能力，并尽量减少人工操作。
- 规则、模板和字段映射应便于维护和迭代。
- 输出结果应便于项目组继续使用、共享和归档。

## 7. 预期收益

- 降低 VFL 历史冗余和重复内容，提高验证列表的准确性与可维护性。
- 建立统一的 VFL 格式，便于跨模块比较、统计、汇总和复用。
- 减少专家对低价值简单条目的重复 review。
- 让专家将时间集中在复杂、高风险和真正需要专业判断的验证内容上。
- 为后续 VFL 的持续维护、质量度量和自动化验证打下基础。

## 8. 后续待确认事项

- 统一 VFL 模板的最终字段定义、字段类型和必填规则。
- 各类测试点的分类、风险等级及专家 review 分级标准。
- 重复和相似测试点的判定规则及人工确认机制。
- “错误”“过时”“无效”等问题的识别依据和责任归属。
- 规范化结果的输出格式、版本管理方式和审批流程。
- 首批试点模块、样例 VFL 文件及验收指标。
- Skill 的调用方式、适用范围、提示词和输出格式规范。
- Spec、HLD、历史 Bug 库的组织方式、版本关联及访问权限。
- Review 报告的存储位置、命名规则、共享方式和版本管理策略。
- 报告中“可直接接受”“建议确认”“必须专家 Review”等分级的定义。

## 9. 可操作性与落地方案

### 9.1 可操作性结论

本项目具备可操作性，但不应以“一次性统一所有模块 VFL 内容”为目标。不同模块的 VFL 在验证对象、专业术语、字段和测试粒度上必然不同，因此应采用“统一最小骨架 + 模块扩展规则 + 人工确认”的方式实施。

可优先自动化的内容包括格式异常、必填项、重复条目、近似重复条目和字段冲突；这类问题有明确规则，结果稳定且容易复核。需要结合 Spec、HLD 和历史 Bug 库判断覆盖完备性或内容正确性的任务，可由大模型生成带证据的初步意见，但必须标记为建议并由专家确认。

### 9.2 设计原则

- 不强制不同模块使用完全相同的业务字段；仅定义可跨模块识别的最小公共字段，如模块、Feature、Test Point、验证目标、优先级、状态和关联文档。
- 将模块专属字段、术语、枚举值、检查规则和风险点放入模块配置，而非固化在程序中。
- 将确定性规则检查、大模型语义分析和专家判断分层处理，避免将大模型输出当作确定事实。
- 每条结论必须回链至 VFL 行、规则、Spec/HLD 章节或历史 Bug 编号；无依据时只输出待确认问题。
- 以只读分析和报告交付为默认方式，任何修改均由用户或专家在原始流程中完成。

### 9.3 现实的分阶段落地路径

#### 阶段一：建立可解析、可追溯的基础能力

选择 2 至 3 个代表性模块和对应历史 VFL 作为试点，先完成 Excel 解析、字段识别、行号追溯和只读报告生成。由项目组确认最小公共字段、模板映射及少量高价值格式规则。

交付结果：格式检查报告、缺失字段清单、文件结构摘要和原始行定位能力。

#### 阶段二：规则检查与重复分析

在试点模块上加入必填项、枚举值、字段依赖、完全重复和文本近似重复检查。相似条目只做分组和差异展示，不自动判定应保留哪一条。

交付结果：重复/相似条目报告、规则异常报告、可由负责人快速处理的低风险问题清单。

#### 阶段三：大模型辅助语义 Review

为每个试点模块沉淀术语表、字段释义、典型 Feature、已确认案例和 Review 提示词。大模型基于单条 VFL、相似条目及模块上下文，输出疑似冲突、描述歧义和待确认结论，并附分析依据、置信度和引用位置。

交付结果：专家重点 Review 清单及带证据的初步检视报告。

#### 阶段四：关联 Spec、HLD 和历史 Bug 库

将版本明确的 Spec、HLD 和历史 Bug 资料建立可检索索引，并按模块、版本和接口关联至 VFL。先从“Bug 是否已有对应测试点”“关键 Spec 条目是否映射至 VFL”两类可验证问题开始，再逐步扩展到覆盖缺口建议。

交付结果：证据驱动的覆盖映射、疑似遗漏项和建议补充的验证方向；所有建议均标记为需专家确认。

#### 阶段五：扩大模块范围与持续优化

根据专家对报告的接受、驳回和修订结论，迭代规则、模块配置和 Skill。新增模块时先配置术语、字段映射和重点风险项，再接入统一 Review 流程。

交付结果：可复用的模块接入流程、规则库、案例库和质量度量看板。

### 9.4 所需工具与技术能力

- Excel 解析工具：读取 `.xlsx` 文件、工作表、表头、单元格格式、合并单元格和行列坐标；建议采用 Python 的 `openpyxl`，必要时配合 `pandas` 处理表格数据。
- 规则引擎与配置文件：以 YAML 或 JSON 维护公共模板、模块字段映射、枚举值、必填规则和检查规则，避免将模块差异写死在代码中。
- 文档解析工具：提取 Spec、HLD、Bug 导出文件中的结构化文本、标题、章节和编号；扫描件或图片型 PDF 需额外具备 OCR 能力。
- 大模型调用能力：支持受控提示词、结构化 JSON 输出、长文本分段、上下文引用和失败重试；模型输出必须保留输入来源和版本信息。
- 检索与关联能力：对 Spec、HLD、历史 Bug 和历史 Review 结论建立全文或向量索引，并按模块和版本过滤检索结果。
- 报告生成工具：生成 Markdown、HTML 或 Excel 格式的只读 Review 报告，包含原始行号、问题类别、依据、置信度、优先级和专家结论栏位。
- 运行与审计能力：记录每次分析的输入文件版本、规则版本、模型版本、提示词版本和输出报告，确保结论可复现和可追溯。

### 9.5 建议沉淀的 Skill

- `vfl-excel-ingestion`：识别 VFL 表结构，抽取统一最小字段，记录原始文件、工作表和行号，并输出解析异常。
- `vfl-format-review`：按公共规则和模块规则检查模板、字段、空值、枚举、数据类型和表格格式。
- `vfl-duplication-review`：执行完全重复、字段级重复和语义近似重复分析，输出分组、差异和待确认项。
- `vfl-semantic-review`：结合模块术语和上下文识别矛盾、歧义、疑似错误及描述不充分的测试点，并要求提供理由。
- `vfl-coverage-review`：基于已检索的 Spec、HLD 和历史 Bug 资料，检查 VFL 映射情况并提出覆盖风险和补充建议。
- `vfl-reporting`：将各类检查结果合并为统一的只读 Review 报告，标注证据、置信度、优先级和人工确认状态。

所有 Skill 都应遵循相同约束：不修改原始 VFL；无法获得充分证据时输出“待专家确认”；引用输入资料的版本和位置；以结构化结果传递给下一步。

### 9.6 试点前置条件与验收建议

- 提供 2 至 3 个具有代表性的模块 VFL，以及已知问题和人工 Review 结论作为评估样本。
- 明确每个试点模块对应的 Spec、HLD、历史 Bug 文件及其版本关系。
- 由专家定义首批高价值规则和“正确、疑似、待确认”的判定标准。
- 以问题定位准确率、重复条目召回率、专家接受率、误报率和专家节省的 Review 时间衡量效果。
- 只有当报告结果经专家验证具有稳定价值后，才扩大模块范围或增加更复杂的覆盖分析能力。
